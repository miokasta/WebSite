const menuButton=document.querySelector('.menu-toggle');const nav=document.querySelector('.site-nav');menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open))});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false')}));
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const track=document.querySelector('.release-track');const scrollCards=direction=>{const card=track.querySelector('.release-card');track.scrollBy({left:direction*(card.offsetWidth+32),behavior:'smooth'})};document.querySelector('.next').addEventListener('click',()=>scrollCards(1));document.querySelector('.prev').addEventListener('click',()=>scrollCards(-1));
const toast=document.querySelector('.toast');let toastTimer;function showToast(message){toast.textContent=message;toast.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>toast.classList.remove('show'),2600)}document.querySelectorAll('[data-platform]').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();showToast(`${link.dataset.platform} link coming soon`) }));document.querySelectorAll('.social-list a').forEach(link=>link.addEventListener('click',event=>{event.preventDefault();showToast(`${link.textContent.replace('↗','').trim()} link coming soon`)}));
document.querySelector('#newsletter-form').addEventListener('submit',event=>{event.preventDefault();const email=document.querySelector('#email');document.querySelector('#form-message').textContent=`You're in — updates will be sent to ${email.value}.`;event.target.reset()});document.querySelector('#year').textContent=new Date().getFullYear();
